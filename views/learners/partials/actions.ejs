<script>
  document.querySelectorAll("button[role='delete']").forEach((button) =>
    button.addEventListener("click", async (event) => {
      event.preventDefault();
      const id = event.target.dataset.id;
      if (id) {
        try {
          const response = await axios.delete(`/api/learners/${id}`);
          console.log("Success:", response);
          // do redirect to refresh the list
          window.location.pathname=("/learners");
        } catch (error) {
          console.error("Error:", error);
        }
      }
    })
  );
</script>
